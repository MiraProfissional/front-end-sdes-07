import { screen } from '@testing-library/react';
import SobreNos from '.';

describe('<SobreNos />', () => {
  it('should render de elements', () => {
    // render (
    //     <SobreNos />
    // )
    // expect(screen.findAllByLabelText('img')).toBeInTheDocument();
  });
});
